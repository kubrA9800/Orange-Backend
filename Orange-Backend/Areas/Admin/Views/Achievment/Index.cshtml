@model AchievmentVM
@{
	ViewData["Title"] = "Index";
}

<div  class="container mt-5">
	<h1 class="text-center mt-3 mb-3">Achievment</h1>
	<div class="table-responsive">
		<table class="table table-striped">
			<thead>
				<tr>
					<th>
						Image
					</th>
					<th>
						Head
					</th>

					<th>
						Results
					</th>
					<th>
						Text
					</th>
					
					<th>
						Created Date
					</th>
					<th>
						Operations
					</th>

				</tr>
			</thead>
			<tbody>

				<tr>
					<td style="white-space:normal">
						<img style="width:150px; height:150px" src="~/assets/img/about/@Model.Image" alt="image" />

					</td>
					<td class="py-1">
						@Model.Head

					</td>

					<td style="white-space:normal">
						@Model.Results

					</td>
					<td style="white-space:normal">
						@Model.Text.Substring(0, (int)Math.Ceiling(Model.Text.Length * 0.1))...
					</td>
					
					<th>
						@Model.CreatedDate.ToString("dd/MM/yyyy")
					</th>
					<td>
						<a class="btn btn-info" asp-action="Detail" asp-route-id="@Model.Id">Detail</a>
						<a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>

				</tr>


			</tbody>
		</table>
	</div>
</div>
